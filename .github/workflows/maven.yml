# This workflow will build a Java project with Maven
# For more information see: https://help.github.com/actions/language-and-framework-guides/building-and-testing-java-with-maven

name: Java CI with Maven

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Set up JDK 1.8
      uses: actions/setup-java@v1
      with:
        java-version: 1.8
    - name: Build with Maven
      run: mvn -B package --file backend/pom.xml -Dmaven.test.skip=true 
#    - name: docker backend 
#      run: docker build . -t backend
#    - name: docker build frontend versioning
#      run: docker build . -t frontend
#    - name: docker login with secret
#    - name: docker push backende and frontend to hockerhub (create incremental counter)
#      run: ---
#    - name:  
